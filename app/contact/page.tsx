import type { Metadata } from 'next';import { ContactForm } from '@/components/ui';import { clinicInfo } from '@/data/clinicInfo';
export const metadata:Metadata={title:'Kontakt',description:'Objednejte konzultaci ve Fresh Vibes v centru Prahy. Telefon, WhatsApp, e-mail a kontaktní formulář.',alternates:{canonical:'/contact'}};
export default function Page(){return <main className="section"><div className="container grid gap-10 md:grid-cols-2"><div><p className="eyebrow">Kontakt</p><h1 className="serif mt-4 text-6xl">Objednat konzultaci</h1><p className="mt-6 text-[#756b66]">Ozveme se s možnostmi termínu. Formulář zatím data neodesílá do CRM — API obsahuje TODO pro napojení backendu.</p><div className="mt-8 grid gap-3"><a className="btn btn-light" href={`tel:${clinicInfo.phone}`}>Call: {clinicInfo.phone}</a><a className="btn btn-light" href={`https://wa.me/${clinicInfo.whatsapp}`}>WhatsApp</a><a className="btn btn-light" href={`mailto:${clinicInfo.email}`}>{clinicInfo.email}</a><a className="btn btn-light" href={clinicInfo.mapsUrl}>Google Maps</a></div></div><ContactForm/></div></main>}
