import { MetadataRoute } from 'next';import { clinicInfo } from '@/data/clinicInfo';import { services } from '@/data/services';export default function sitemap():MetadataRoute.Sitemap{const routes=['','/services','/about','/contact','/blog','/privacy','/cookies','/legal',...services.map(s=>`/services/${s.slug}`)];return routes.map(r=>({url:`${clinicInfo.baseUrl}${r}`,lastModified:new Date(),changeFrequency:'monthly',priority:r===''?1:.7}))}
