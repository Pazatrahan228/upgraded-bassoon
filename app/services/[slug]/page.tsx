import type { Metadata } from 'next';import { notFound } from 'next/navigation';import Link from 'next/link';import { getService, services } from '@/data/services';import { ContactForm, Photo } from '@/components/ui';import { clinicInfo } from '@/data/clinicInfo';
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}
export function generateMetadata({params}:{params:{slug:string}}):Metadata{const s=getService(params.slug); if(!s)return {}; return {title:s.title,description:`${s.short} Fresh Vibes — zubní klinika Praha. ${s.price}.`,alternates:{canonical:`/services/${s.slug}`},openGraph:{title:s.title,description:s.short}}}
export default function Page({params}:{params:{slug:string}}){const s=getService(params.slug); if(!s)notFound(); const crumbs={'@context':'https://schema.org','@type':'BreadcrumbList',itemListElement:[{ '@type':'ListItem',position:1,name:'Služby',item:`${clinicInfo.baseUrl}/services`},{'@type':'ListItem',position:2,name:s.title,item:`${clinicInfo.baseUrl}/services/${s.slug}`} ]};return <main><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(crumbs)}}/><section className="container grid gap-10 py-16 md:grid-cols-2"><div><Link href="/services" className="eyebrow">← Služby</Link><h1 className="serif mt-5 text-6xl">{s.title}</h1><p className="mt-6 text-xl text-[#756b66]">{s.description}</p><p className="mt-6 font-bold">{s.price}</p><div className="mt-8 flex gap-3"><Link href="/contact" className="btn btn-dark">Objednat konzultaci</Link><Link href="/about" className="btn btn-light">O klinice</Link></div></div><Photo label={`${s.title} procedure`}/></section><section className="section bg-[#f4eeee]"><div className="container grid gap-8 md:grid-cols-2"><div><h2 className="serif text-4xl">Co očekávat</h2><ul className="mt-6 space-y-3 text-[#756b66]"><li>• konzultace a individuální plán</li><li>• transparentní komunikace možností</li><li>• doporučení lékaře podle vyšetření</li></ul></div><ContactForm/></div></section></main>}
